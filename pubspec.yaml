name: pab_tugas_besar
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.8 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_sign_in: ^7.0.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  firebase_messaging: ^14.7.19
  
  # 🎯 UBAH JADI VERSI 17 DI SINI, SYAH:
  flutter_local_notifications: ^17.0.0

  provider: ^6.1.2
  http: ^1.2.1
  shared_preferences: ^2.5.5
  logger: ^2.4.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_Pgerd.png
    - assets/google_logo.png
    - assets/layar.png
    - assets/me.png